extends Area2D

export (int) var speed = 120
export (int) var jump_speed = -180
export (int) var gravity = 400

enum state (FALL, IDLE, JUMP)



func _ready():
	pass


func _on_Timer_timeout():
	pass # Replace with function body.
